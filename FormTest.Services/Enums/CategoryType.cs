namespace FormTest.Services.Enums
{
    public enum CategoryType
    {
        // By default, the first one is 0, next is 1, etc.
        // But we can assign specific numbers if we want.
        CategoryX = 1,
        CategoryY = 2,
    }
}
